# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_GB_API_KEY: "${{ secrets.NEXT_PUBLIC_GB_API_KEY }}"
      NEXT_PUBLIC_FIREBASE_API_KEY: "${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}"
      NEXT_PUBLIC_FIREBASE_AUTH: "${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH }}"
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: "${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}"
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}"
      NEXT_PUBLIC_FIREBASE_MSG_SENDER_ID: "${{ secrets.NEXT_PUBLIC_FIREBASE_MSG_SENDER_ID }}"
      NEXT_PUBLIC_APP_ID: "${{ secrets.NEXT_PUBLIC_APP_ID }}"
      NEXT_PUBLIC_FIRESTORE: "${{ secrets.NEXT_PUBLIC_FIRESTORE }}"

    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SUGGESTION_BOX_3A08F }}'
          channelId: live
          projectId: suggestion-box-3a08f
